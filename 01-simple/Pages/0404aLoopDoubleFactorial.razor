@page "/0404aLoopDoubleFacorial"

<h1>Double facorial</h1>
<br/>
<table>
    <tr>
        <td>Number:</td>
        <td>
            <input style="text-align:right" @bind="number"/>
        </td>
    </tr>
    <tr>
        <td style="text-align:center" colspan="2">
            <button title="double factorial" @onclick="CalculateDoubleFactorial">!!</button>
        </td>
    </tr>
    <tr>
        <td>@number!!&nbsp;=&nbsp;</td>
        <td><input style="text-align:right" @bind="result"/></td>
    </tr>
 </table> 
<br/>

@code {

    private int number = 5;

    private double result = 1;

    private void CalculateDoubleFactorial()
    {
        int n = number;
        if ( n <= 0)
        {
            result = -1;
            return;
        }
        result = 1;
        if( n%2 == 0)
        {   // for even n
            int i = 2;
            while( i <= n )
            {
                result *= i;
                i += 2;
            }
        }
        else
        {   // here n is odd
            int i = 1;
            do
            {
                result *= i;
                i += 2;
            } while(i <= n);
        }
    }


}
